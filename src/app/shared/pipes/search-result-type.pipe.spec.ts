import { SearchResultTypePipe } from './search-result-type.pipe';

describe('SearchResultTypePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchResultTypePipe();
    expect(pipe).toBeTruthy();
  });
});
