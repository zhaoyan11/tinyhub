import { DistanceInCNPipe } from './distance-in-cn.pipe';

describe('DistanceInCNPipe', () => {
  it('create an instance', () => {
    const pipe = new DistanceInCNPipe();
    expect(pipe).toBeTruthy();
  });
});
